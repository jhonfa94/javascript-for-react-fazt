export const add = (x, y) => {
  return x + y;
};

export const multiply = (x, y) => {
  return x * y;
};

export const title = "js for react";
export const active = true;
export const points = [10, 20, 30];

const log = () => {
  console.log("Export por defecto");
};

export default log;
